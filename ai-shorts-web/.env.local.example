# Supabase — Project Settings -> API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Anthropic — for script drafting, later QA vision + SEO generation
ANTHROPIC_API_KEY=

# Inngest — Settings -> Event Keys / Signing Key (only needed for prod; local
# dev works against http://localhost:8288 with no keys via `npx inngest-cli dev`)
INNGEST_EVENT_KEY=
INNGEST_SIGNING_KEY=

# Filled in later (Build order steps 4-5)
RENDER_WORKER_URL=
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
